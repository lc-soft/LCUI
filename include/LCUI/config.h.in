#define PACKAGE_VERSION "${VERSION}-${GIT_COMMIT}"

${define HAVE_FREETYPE}
${define HAVE_FONTCONFIG}

${define HAVE_LIBJPEG}
${define HAVE_LIBPNG}

${define WITH_LIBXML2}

${define HAVE_LIBX11}
${define ENABLE_TOUCH}

${define ENABLE_OPENMP}
