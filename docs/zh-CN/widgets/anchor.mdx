# Anchor

链接部件，用法与 HTML 的 `<a>` 标签相同，用于打开外部 URL。

<WidgetExample name="anchor-basic" />

## 适用场景

- **适用** — 在页面中放置外部链接（点击后在系统浏览器中打开）。
- **不适用** — 需要执行动作的按钮（用 `button`）。

## API 参考

<FieldTable name="anchor" />
