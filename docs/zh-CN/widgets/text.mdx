# Text

用于显示文本的部件。

<WidgetExample name="text-basic" />

## 适用场景

- **适用** — 静态文本显示（标题、标签、说明文字）。
- **不适用** — 需要用户编辑文本（用 `textinput`）。

## 用法

```tsx
import { Text } from "@lcui/react"
```

```tsx
<Text>Hello</Text>
```

## API 参考

<FieldTable name="text" />
