# TextInput

可编辑的文本输入部件。

<WidgetExample name="text-input-basic" />

## 适用场景

- **适用** — 用户输入文本（表单字段、搜索框）。
- **不适用** — 仅需要展示文本（用 `text`）。

## 用法

```tsx
import { TextInput } from "@lcui/react"
```

```tsx
<TextInput />
```

## API 参考

<FieldTable name="text-input" />
