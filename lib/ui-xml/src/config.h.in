#define LIBUI_XML_VERSION "${VERSION}"
#define LIBUI_XML_VERSION_MAJOR ${VERSION_MAJOR}
#define LIBUI_XML_VERSION_MINOR ${VERSION_MINOR}
#define LIBUI_XML_VERSION_ALTER ${VERSION_ALTER}
${define LIBUI_XML_STATIC_BUILD}
${define LIBUI_XML_HAS_LIBXML2}
