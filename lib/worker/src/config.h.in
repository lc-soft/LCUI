#define LIBWORKER_VERSION "${VERSION}"
#define LIBWORKER_VERSION_MAJOR ${VERSION_MAJOR}
#define LIBWORKER_VERSION_MINOR ${VERSION_MINOR}
#define LIBWORKER_VERSION_ALTER ${VERSION_ALTER}
${define LIBWORKER_STATIC_BUILD}
