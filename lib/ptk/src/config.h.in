#define PTK_VERSION "${VERSION}"
#define PTK_VERSION_MAJOR ${VERSION_MAJOR}
#define PTK_VERSION_MINOR ${VERSION_MINOR}
#define PTK_VERSION_ALTER ${VERSION_ALTER}
${define PTK_STATIC_BUILD}
${define PTK_HAS_LIBX11}
