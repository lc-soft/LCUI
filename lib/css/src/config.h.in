#define LIBCSS_VERSION "${VERSION}"
#define LIBCSS_VERSION_MAJOR ${VERSION_MAJOR}
#define LIBCSS_VERSION_MINOR ${VERSION_MINOR}
#define LIBCSS_VERSION_ALTER ${VERSION_ALTER}
${define LIBCSS_STATIC_BUILD}
